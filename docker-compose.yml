version: '3.8'

services:
  pacelab-vdot:
    build: .
    container_name: running_pro_app
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - GEMINI_API_KEY=${GEMINI_API_KEY}
